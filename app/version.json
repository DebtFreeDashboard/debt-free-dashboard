{
  "version": "1.6.0",
  "releaseDate": "2026-04-19",
  "releaseName": "Release D — Data Integrity",
  "notes": [
    "sanitizeDebts() helper shared by load() and importData() — old-format backups now backfill postPromoApr/promoApr/promoEnd/hasPromo/transferFee/type cleanly.",
    "importData() no longer skips sanitization — imported JSON goes through the same pipeline as load().",
    "save() surfaces storage errors (including QuotaExceededError) instead of silently dropping writes.",
    "projectBalancesAt() rewritten for parity with simulate() — promo-APR handling, safeMinPayment, MAX_MONTHS cap. Lump-allocation preview now matches the main simulator."
  ]
}
