{
  "version": "1.4.0",
  "releaseDate": "2026-04-18",
  "notes": "Release B — avalanche sort now uses max(realApr, postPromoApr, apr); pending Payment Log entries feed into forecasts via plannedDisbursements(); What-If monotonicity clamp removed (proven dead code); renderStrategy timeline sort aligned with simulate()."
}
